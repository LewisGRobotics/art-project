import { AddViewportPipe } from './add-viewport.pipe';

describe('AddViewportPipe', () => {
  it('create an instance', () => {
    const pipe = new AddViewportPipe();
    expect(pipe).toBeTruthy();
  });
});
